# .pre-commit-config.yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0  # Use the latest stable ruff version
    hooks:
      # Run the linter
      - id: ruff
        args: [--fix]
      # Run the formatter (optional)
      - id: ruff-format